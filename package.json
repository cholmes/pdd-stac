{
  "name": "stac-templates",
  "version": "1.0.0",
  "description": "STAC HTML Template development examples",
  "main": "./src/index.html",
  "dependencies": {
    "bootstrap": "^4.0.0",
    "ol": "^4.6.4",
    "ol-hashed": "^1.0.2",
    "parcel-plugin-handlebars": "^0.4.4"
  },
  "devDependencies": {
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.2.1"
  },
  "scripts": {
    "start": "parcel src/pages/index.hbs --no-cache",
    "build": "parcel src/pages/index.hbs  --no-cache --public-url /",
    "clean": "rm -rf dist && rm -rf .cache"
  }
}
